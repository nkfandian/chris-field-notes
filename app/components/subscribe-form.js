'use client'
import {useState} from 'react'
export default function SubscribeForm(){const [notice,setNotice]=useState(''),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setNotice('');const form=new FormData(e.currentTarget),response=await fetch('/api/subscribe',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:form.get('email'),website:form.get('website')})}),data=await response.json();setBusy(false);setNotice(data.message||data.error||'暂时无法订阅，请稍后再试。');if(response.ok)e.currentTarget.reset()}return <form className="subscribe-form" onSubmit={submit}><label htmlFor="subscribe-email">EMAIL ADDRESS</label><input className="form-honeypot" name="website" tabIndex="-1" autoComplete="off" aria-hidden="true"/><div><input id="subscribe-email" name="email" type="email" autoComplete="email" placeholder="you@example.com" required/><button disabled={busy}>{busy?'提交中…':'订阅更新 →'}</button></div><p aria-live="polite">{notice}</p></form>}
